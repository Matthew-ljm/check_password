{
  "version": 2,
  "builds": [
    { "src": "verify.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "verify.js" }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
